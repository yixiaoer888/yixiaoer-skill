# 平台分类说明 (Platform Category)

本页用于兼容旧平台文档中的 `platform-category.md` 引用。

## 当前建议

- 通用分类字段优先读取 [`./get-publish-categories.md`](./get-publish-categories.md)
- 若平台文档要求层级分类，必须保留完整路径对象，不要只填叶子 ID
- 具体平台差异仍以对应平台页和 `schema get` 结果为准
